services:
  - type: web
    name: api-central-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --timeout 240 --graceful-timeout 30
    autoDeploy: true
    envVars:
      - key: KICK_API_URL
        value: https://kick-duelo-api.onrender.com
      - key: WARZONE_API_URL
        value: https://warzone-api-qbn9.onrender.com
      - key: REDSEC_API_URL
        value: https://redsec-loadout-api.onrender.com
